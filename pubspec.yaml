name: helm_api
description: A small, typed, Dio-first API client for Dart and Flutter.
version: 0.1.0
repository: https://github.com/Daskerwow/helm_api

environment:
  sdk: ^3.13.2

dependencies:
  dio: ^5.11.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
